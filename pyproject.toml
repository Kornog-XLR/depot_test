[project]
name = "depot-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kornog-XLR", email = "nicolas.stephan2024@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
depot-test = "depot_test:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
